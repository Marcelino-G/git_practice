console.log("my first commit with Git!")
console.log("so far so good!")
console.log("lets see if we can backtrack.")
console.log("messed around a bit with 'checkout', wasn't able to 'push' without getting errors... 'head detached'?")